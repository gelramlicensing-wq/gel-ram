- [ ] I have completed the GEL RAM CLA privately with the project before opening this pull request.
- [ ] `cargo run --locked --offline -p xtask -- verify` passes on this branch.
- [ ] For a performance change: before/after benchmark output is included below and no exactness test is weakened.

CI checks the first line. It accepts the ticked box as `[x]` or `[X]` and re-checks the line when the pull request description is edited. Tick it only if the completed CLA is on file with the project (`CLA.md`, `CONTRIBUTING.md`). A pull request without it is closed unmerged.

## Summary

## Benchmark output (performance changes only)

```text
before:
after:
```
